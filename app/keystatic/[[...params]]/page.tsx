import KeystaticApp from "../keystatic";

// The /keystatic admin panel. It renders entirely on the client;
// the header/footer are hidden for this route via the layout below.
export default function Page() {
  return <KeystaticApp />;
}
